import { createTheme } from "@mui/material/styles";

const theme = createTheme({
	palette: {
		primary: {
			main: "#0D1F2D",
		},
		secondary: {
			main: "#388659",
		},
		background: {
			main: "#F2F7F2",
			default: "#F2F7F2",
		},
	},
});

export default theme;
